import { describe, expect, it } from 'vitest';
import { DayImpl } from './day{{day}}.ts';

describe('day{{day}}', () => {
  describe('partOne', () => {
    it('should return', () => {
      const sample = ``;

      expect(new DayImpl(sample).partOne()).toEqual();
    });
  });

  describe('partTwo', () => {
    it('should return', () => {
      const sample = ``;

      expect(new DayImpl(sample).partTwo()).toEqual();
    });
  });
});
